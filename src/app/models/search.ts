export interface Search {
    minAge: number,
    maxAge: number,
    minDay: number,
    maxDay: number,
    happy: boolean,
    coping: boolean,
    sad: boolean,
    male: boolean,
    female: boolean,
}